name: desenvolverapp
description: App para Analise de desempenho de atividades

version: 1.0.0+2

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^0.4.0+5
  firebase_auth: ^0.11.1+6
  cloud_firestore: ^0.12.5+1
  modal_progress_hud: ^0.1.3
  flutter_speed_dial: ^1.0.9
  cupertino_icons: ^0.1.2
  url_launcher:  '>=0.4.2 < 5.2.1'
  intl: ^0.16.0
  date_format: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - images/